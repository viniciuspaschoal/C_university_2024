/*Escreva um programa em C que leia um vetor de cinco inteiros e 
use um apontador para copiar seus valores para outro vetor de 
inteiros. Ao final mostre os valores do segundo vetor.*/

#include <stdio.h>
#include <stdlib.h>

int main(vois){
	int i, *apt, vet1[5], vet2[5];
	
	for(i = 0; i < 5; i++){
		printf("Digite o %d numero: ", i+1);
		apt = vet1[i];
		s
		canf("%d", apt);
		printf("%d", vet1[i]);
	}
	return 0;
}
